# TechAuditPros — context
1. Repo = techauditpros.com (Vercel) + ajipaul1.github.io; static HTML, CRLF, no build; commit to main as Aji Paul with trailer `Co-Authored-By: Claude <model> <noreply@anthropic.com>`; owner's GitHub Desktop auto-pushes "yy" mid-session (diff against a base commit, never `git checkout --`).
2. Content: never duplicate a page across regions/cities; new page only with real SEMrush volume + a crawl of the current #1; own canonical + hreflang; root-absolute links; visible FAQ text = FAQPage schema; real figures only, no fake data, no secrets, no competitor names; archive removed content, never delete.
3. Design: copy /ca/erp/ (service), /ca/toronto/ (hub) or the blog generator; palette tokens from the page `:root` only; IBM Plex Sans/Mono; mobile-first + wide tier (1440→1480px, 1850→1720px); images from assets/images/library (CATALOG.md, update Used-on); animation layers are scripts (cinematic.js on /us/*, imax.js on /us/), reduced-motion respected.
4. Tools: `node scripts/verify/server.js` then `node scripts/verify/shot.js us/ name 1440 0 [selector]` (MOTION=1 = real motion; read its text output, ≤2 viewport shots, none if told to stop); generators in scripts/ — blog `run_posts.js`→`build_bloghub.js`, country-pages `us_*.js`, `nav_patch.js`, `cinematic.js`→`imax.js` (`--strip` first to redo cinematic), `gen_sitemap.js` after any page change, `update_catalog.js`; grep/sed pages, never cat.
5. Before every commit add one dated line to PROGRESS.md. Originals with rationale: archive/ai_context-verbose-2026-09-03/.
