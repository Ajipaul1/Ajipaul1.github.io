# TechAuditPros (techauditpros.com) — AI bootstrap

Read, in order, before any task — and nothing else up front:
1. `ai_context/CONTEXT.md` — rules, design system, how to work (one page).
2. `ai_context/PROGRESS.md` — what is live, the open queue, one-line session log.

Read page files with grep/sed, never whole. Owner = Aji Paul, informal typo-heavy English — read for intent; state your understanding first only for large or costly requests. No "Approved" gate: act once the request is clear, verify locally (text checks first, at most 2 viewport screenshots), append one dated line to PROGRESS.md, commit, push to main.

Verbose originals of the old context docs (with rationale) live in `archive/ai_context-verbose-2026-09-03/` — consult only when a rule's reasoning is needed. Keep CONTEXT.md and PROGRESS.md accurate and short as the site evolves.
